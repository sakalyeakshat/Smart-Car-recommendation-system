# Frontend Environment Variables
# Copy this file to .env and update the values as needed.
# When running via Docker Compose, these are set automatically — no .env file required.

# Note: REACT_APP_API_URL is no longer needed.
# All API calls (/recommend, /explore) use relative URLs which are proxied
# through the React dev-server to the backend (see proxy in package.json).
